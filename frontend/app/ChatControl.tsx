import React from 'react';
import RobotClient from './robot_client';

interface Props {
  robotClient: RobotClient;
}

export default function ChatControl({ robotClient }: Props) {
  return <div>TODO: Chat Control</div>;
};
