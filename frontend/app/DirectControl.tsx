import React from 'react';
import RobotClient from './robot_client';

interface Props {
  robotClient: RobotClient;
}

export default function DirectControl({ robotClient }: Props) {
  return <div>TODO: Direct Control</div>;
};
