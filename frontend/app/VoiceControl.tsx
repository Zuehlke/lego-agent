import React from 'react';
import RobotClient from './robot_client';

interface Props {
  robotClient: RobotClient;
}

export default function VoiceControl({ robotClient }: Props) {
  return <div>TODO: Voice Control</div>;
};
